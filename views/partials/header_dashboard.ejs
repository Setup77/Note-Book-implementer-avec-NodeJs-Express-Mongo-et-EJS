 <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark container-fluid mb-5">
    <div class="container">
      <!-- Logo -->
       <% if (session && session.user) { %>
   <a class="navbar-brand" href="/dashboard"> <%= session.user.firstName %></a>
<% }  %>
      <!-- Bouton hamburger -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Contenu du menu -->
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <li class="nav-item"><a href="/users" class="nav-link">Membres</a></li> 
        
          <li class="nav-item"><a class="nav-link" href="#">Users</a></li>
          <li class="nav-item"><a class="nav-link" href="#">Note Users</a></li>
    

        <!-- Barre de recherche -->
        <form class="d-flex end" role="search" method="POST" action="/dashboard/search">
          <input class="form-control me-2" type="search"  name="searchTerm" placeholder="Rechercher..." aria-label="Search">
       
        </form>

           <% if (session && session.user) {  %>

      <li class="nav-item">
        <a href="/logout" title="Déconnexion" class="nav-link">Déconnexion</a>
      </li>
      <% } else { %>
     <li class="nav-item">
        <a href="../register" title="inscription" class="nav-link">S'inscrire</a></li>
       <li class="nav-item">
    <a href="../login" title="Connexion" class="nav-link">Se connecter</a>
      </li>
      <% } %>
 </ul>

      </div>
    </div>
  </nav>

<!-- Bouton Retour en haut -->
<button type="button" id="btnScrollTop" class="btn btn-primary rounded-circle shadow position-fixed">
  <i class="bi bi-arrow-up fw-bold font-weight "></i>
</button>


<!-- Navbar principale -->

  <nav class="navbar navbar-dark bg-dark px-3">
    <!-- Bouton Toggle à gauche -->
    <button id="BoutonMenuMobiel" class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileMenu" aria-controls="mobileMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Logo -->
    <a class="navbar-brand  <%= locals.page == 1 ? 'fw-bold' : '' %>  ms-2" href="/">
      <img src="http://localhost:5000/img/nkw.png" width="32" height="32" title="Accueil"  alt="Logo" class="d-inline-block align-text-top me-2">
      Note-Book
    </a>
          <!-- Liens de gauche -->
    <ul class="navbar-nav d-none d-lg-flex flex-row ms-auto">
        <li class="nav-item me-5" >
            <% if (session && session.user) { %>
   <a class="nav-link <%= locals.page == 6 ? 'active fw-bold' : '' %>"  active fw-bold" href="/dashboard"> <i class="fa fa-user"></i> <%= session.user.firstName %></a>
<% }  %>
        </li>
           <li class="nav-item me-5"><a href="/users"  class="nav-link <%= locals.page == 4 ? 'active' : '' %>"> <i class="fa fa-users"></i> Membres</a></li> 
        
           <li class="nav-item"><a href="/consulter-tous-les-notes" title="Tous les notes des utilisateurs" class="nav-link <%= locals.page === '7' ? 'active' : '' %>"><i class="fa fa-eye"></i> Consulter</a></li> 
      
      </ul>
 <% if (locals.page == 6 || locals.page == 7 || locals.page == 4) { %>
    <!-- Barre de recherche (centrée sur desktop) -->
    <form class="d-none d-lg-flex mx-auto" role="search">
      <input class="form-control search-bar" id="search"  type="search" placeholder="<%= locals.search  %>" aria-label="Search">
    </form>

    <% }  %>

    <!-- Liens de droite -->
    <ul class="navbar-nav d-none d-lg-flex flex-row ms-auto">
         <% if (session && session.user) {  %>
  <li class="nav-item me-5">
    <a class="nav-link <%= locals.page == 5 ? 'active fw-bold' : '' %>"  active fw-bold" href="/profil-<%= session.user._id %>"> <i class="fa fa-user"></i> Profil</a>
</li>
          
      <li class="nav-item me-5">
        <a href="/logout" title="Déconnexion" class="nav-link <%= locals.page == 3 ? 'active' : '' %>"><i class="fa fa-sign-out"></i> Déconnexion</a>
      </li>
      <% } else { %>
         <li class="nav-item me-5">
    <a href="../login" title="Connexion" class="nav-link <%= locals.page == 3 ? 'active' : '' %>"><i class="fa fa-sign-in"></i> Se connecter</a>
      </li>
     <li class="nav-item">
        <a href="../register" title="inscription" class="btn  <%= locals.page == 2 ? 'btn-outline-light' : 'btn-primary' %>"><i class="fa fa-user"></i> S'inscrire</a></li>
      
      <% } %>
    </ul>
  </nav>

  <!-- MENU OFFCANVAS (mobile) -->
 <div class="offcanvas offcanvas-start text-white" tabindex="-1" id="mobileMenu" aria-labelledby="mobileMenuLabel">
    <div class="offcanvas-header border-bottom">
      <h5 class="offcanvas-title" id="mobileMenuLabel">Sasort</h5>
      <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Fermer"></button>
    </div>
    <div class="offcanvas-body">
      <!-- Liens du menu -->
      <ul class="navbar-nav">
        <li class="nav-item"> <a class="nav-link text-white <%= locals.page == 1 ? 'active' : '' %>" href="/"><i class="fa fa-home"></i> Accueil</a></li>
        <hr class="border-secondary"> 
        <% if (session && session.user) { %>
   <a class="nav-link text-white fw-bold <%= locals.page == 6 ? 'active' : '' %>" href="/dashboard"><i class="fa fa-user"></i> <%= session.user.firstName %></a>
<hr class="border-secondary">
   <% }  %>
  <li class="nav-item"><a href="/users" class="nav-link text-white <%= locals.page == 4 ? 'active' : '' %>"> <i class="fa fa-users"></i> Membres</a></li> 
       <hr class="border-secondary">  
  <li class="nav-item"><a href="/consulter-tous-les-notes" title="Tous les notes des utilisateurs" class="nav-link text-white <%= locals.page == 7 ? 'active' : '' %>"><i class="fa fa-eye"></i> Consulter</a></li> 
      </ul>
      <% if (locals.page == 6 || locals.page == 7 || locals.page == 4) { %>
<hr class="border-secondary">
      <!-- Barre de recherche -->
      <form class="mt-3" role="search">
        <input class="form-control" id="searchInputOffcanvas" type="search" placeholder="<%= locals.search  %>" aria-label="Search">
      </form>
  <% }  %>
      <!-- Liens de connexion -->
      <hr class="border-secondary">
              <% if (session && session.user) {  %>         
    <a class="nav-link <%= locals.page == 5 ? 'active fw-bold' : '' %>"  active fw-bold" href="/profil-<%= session.user._id %>"> <i class="fa fa-user"></i> Profil</a>
       <hr class="border-secondary">
    <a href="/logout" title="Déconnexion" class="btn btn-outline-light w-100 mb-2"><i class="fa fa-sign-out"></i> Déconnexion</a>

      <% } else { %>
    <a href="../login" title="Connexion" class="btn btn-outline-light w-100 mb-2 <%= locals.page == 3 ? 'active' : '' %>"><i class="fa fa-sign-in"></i> Se connecter</a>
        <a href="../register" title="inscription" class="btn  <%= locals.page == 2 ? 'btn-outline-light' : 'btn-primary ' %> w-100"><i class="fa fa-user"></i> S'inscrire</a>
      
      <% } %>
    </div>
  </div>